import random
n = 1
money = 10000
while n <= 20:
    num = random.randint(1, 11)
    if num < 5:
        print(f"员工{n},绩效分{num},低于5，不发工资，下一位。")
    else:
        money -= 1000
        print(f"向员工{n},发放工资1000元,账户余额还剩{money}元。")
        if money == 0:
            break
    n += 1
